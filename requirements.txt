# Versiones fijadas el 2026-08-21 con el build verificado en produccion
# (sitio + docs/Libro.pdf con mermaid pre-renderizado). Al actualizar alguna,
# comprobar el PDF, no solo el sitio: es la parte mas fragil de la cadena.

# Generacion del PDF. weasyprint y pydyf van acoplados de cerca: desalinear
# sus versiones rompe el PDF de forma silenciosa.
weasyprint == 62.3
pydyf == 0.10.0
mkdocs-with-pdf == 0.9.3
# Usado directamente por hooks.py (_shrink_if_tall) para reescalar diagramas mermaid muy altos
# antes de entregarlos a WeasyPrint. Ya llega como dependencia transitiva de weasyprint, pero se
# fija aqui por ser una importacion directa del hook.
Pillow >= 9.1.0

# Nucleo
mkdocs == 1.6.1
mkdocs-material == 9.7.7
mkdocs-material-extensions == 1.3.1
mkdocs-get-deps == 0.2.2

# Plugins
mkdocs-carbon == 2.1.0
mkdocs-github == 0.0.6
mkdocs-minify-plugin == 0.8.0
mkdocs-redirects == 1.2.3
mkdocs-git-revision-date-localized-plugin == 1.5.4
mkdocs-extra-sass-plugin == 0.1.0
mkdocs-video == 1.5.0
mkdocs-jupyter == 0.26.3

# Utilidades
livereload == 2.7.1
qrcode == 8.2
pyyaml == 6.0.3
